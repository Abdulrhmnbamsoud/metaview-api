# =========================
# MetaView API - requirements.txt (Railway stable)
# =========================

# --- FastAPI stack ---
fastapi==0.115.6
uvicorn[standard]==0.30.6
pydantic==2.9.2
starlette==0.41.3

# --- Networking / async ---
httpx==0.27.2
anyio==4.7.0
sniffio==1.3.1

# --- RSS parsing ---
feedparser==6.0.11

# --- Scraping / HTML extraction ---
trafilatura==1.9.0
beautifulsoup4==4.12.3
lxml==5.3.0
readability-lxml==0.8.1

# --- Utilities ---
python-dotenv==1.0.1
python-multipart==0.0.12
certifi==2024.12.14
charset-normalizer==3.4.0
idna==3.10

# --- Optional performance (Linux only on Railway) ---
uvloop==0.21.0; platform_system != "Windows"
httptools==0.6.4; platform_system != "Windows"
watchfiles==0.24.0; platform_system != "Windows"

# --- Logging (optional but useful) ---
loguru==0.7.2
